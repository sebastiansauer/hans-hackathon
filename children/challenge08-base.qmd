

**Interaktion mit dem LLM**

1. Wie oft wird (pro Monat) ein `AI transcript` angefordert?
2. Wie oft wird (pro Monat) mit dem LLM interagiert?
3. Welcher Anteil der Besucher (`visitors`) interagiert mit dem LLM?
